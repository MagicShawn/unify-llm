# 安全修复复检报告（2026-09-13）

对象：commit `cf819a8` "security: fix F1-F8 from z-code audit"
方法：修复代码 diff 审查 + 全量测试 + 两套实机审计（原 25 项回归 + 新增 5 项对抗性检查）

## 结论

F1–F8 全部修复有效，回归 **25 checks / 0 findings**，全量 pytest **127 passed**。
对抗性复检发现 **1 个修复引入的新缺陷（R4，低危）**——**已修复**（`fix/scrypt-verify-500`）。

## R4 修复记录

- 根因：`verify_password` 对通过 n/r/p 上限、但 `128*N*r > maxmem` 的篡改哈希仍调用 `hashlib.scrypt`，抛 `ValueError` → 登录 HTTP 500。
- 处理：参数解析与 scrypt 计算一并包进 try/except；并前置拒绝内存需求超过 `_SCRYPT_MAXMEM` 的组合。
- 验证：`tests/test_security.py`（单元 + HTTP 登录）+ `audit_fix_regressions.py` R4 → 401，全量 **128 passed**。

## 回归结果（audit_live.py，25 项）

- B2/B3/B4-FIXED：LAN 伪造 XFF 调 admin API / 建管理员 / 造 key → 403，接管不可再复现
- C5-FIXED：登录爆破第 11 次（账号阈值 10）即 429
- C6-FIXED：pending 与未知邮箱统一 401，枚举消除
- C10-FIXED：改密后旧会话 401，当前设备保持 200
- C13-FIXED：轮换 XFF 0/15 通过（限流桶键改为 TCP peer）
- A5-FIXED：会话令牌仅存 SHA-256，迁移后明文从 DB 消失
- 其余（认证矩阵、积分、SQLi、RBAC、禁用吊销、掩码）全部保持 PASS

## 对抗性补充（audit_fix_regressions.py）

| # | 结果 | 说明 |
|---|------|------|
| R1 | PASS | 旧 N=2^14 哈希仍可验证，升级兼容 |
| R2 | PASS | 旧明文 session 迁移为哈希，旧 cookie 依然有效，明文已从库中清除 |
| R3a | PASS | trusted_proxies 正向路径正确：可信代理后的 XFF 被采纳用于归因/限流 |
| R4 | **PASS（已修复）** | 篡改 over-maxmem scrypt 行 → 登录 401，不再 500 |
| R5 | PASS | 密码长度边界（<10、>128）在注册/改密均被拒 |

### R4 修复（unify_llm/users.py verify_password）

```python
    try:
        # parse + cap checks + refuse 128*n*r > maxmem
        dk = hashlib.scrypt(...)
    except (ValueError, TypeError, MemoryError, OSError):
        return False
    return hmac.compare_digest(dk, expected)
```

严重性：低（利用前提是能写 users DB，那已是更深的失陷）；已消除登录可用性污染。

## 遗留的非缺陷权衡（知悉即可）

1. LoginGuard 按账号锁定：任何人可对任意邮箱灌 10 次失败使其锁定 30s（账号拒绝服务）。LAN 场景可接受；如需缓解可改为"账号计数只在 IP 可信时锁定"。
2. 共享 NAT 出口多个用户共享 30 次/分钟/IP 的失败配额。
3. scrypt N=2^15 仍低于 OWASP 2^17 建议（commit 注释已说明是登录延迟权衡，格式带参数、后续可平滑上调）。
4. cookie `Secure` 默认关（HTTP LAN 设计取舍），已提供 `auth.session_cookie_secure` + TLS 文档路径。
5. `scripts/merge_pr_tmp.py`（未跟踪）是触碰 git credential 的临时运维脚本——**已删除，勿提交**。

## 复检材料

- `security_review/audit_live.py` + `audit_log_v2.txt`（25 项回归，0 findings）
- `security_review/audit_fix_regressions.py`（5 项对抗性检查，R4 已转 PASS）
- `python -m pytest tests -q` → **128 passed**
